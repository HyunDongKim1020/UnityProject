using UnityEngine;
using System.Collections;

public class HP_del : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
    void Delete()
    {
        Destroy(gameObject);
    }
}
